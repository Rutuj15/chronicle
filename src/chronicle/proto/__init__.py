"""Generated gRPC stubs for Chronicle's client / server / worker protocol.

The ``chronicle_pb2`` and ``chronicle_pb2_grpc`` modules are produced from
``chronicle.proto`` by ``tools/gen_proto.py``; do not edit them by hand. They are
checked in so ``uv run pytest`` works with no codegen prerequisite, mirroring how
the rest of the project has no build step. Regenerate after editing the
``.proto``.
"""
