# Python
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv/

# Tool caches (created by ruff / mypy / pytest)
.mypy_cache/
.pytest_cache/
.ruff_cache/

# Test coverage
.coverage
.coverage.*
htmlcov/
coverage.xml

# SQLite databases — Week 2 persistence artifacts (never commit binary DBs)
*.db
*.db-journal
*.sqlite
*.sqlite3

# Environment / secrets (Postgres, Redis, gRPC creds in later weeks)
.env
.env.*
!.env.example

# OS / editor
.DS_Store
.idea/
.vscode/

# Claude Code context
CLAUDE.md

# Scratch files for testing code snippets, etc.
scratch
